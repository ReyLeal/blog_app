<h1> Post </h1>

<div class="field">

  <% @posts.each do |p| %>


  Title: <%= p.title %>
  Body: <%= p.body %>
  Comments: <%= p.comments[0].body %>
  <a href="<%= post_path(p)%>">Show</a>

  <%end%>

</div>
